@model BulgarianWines.Web.ViewModels.ShoppingBagAndFavorites.FavoritesViewModel

<div class="header-cart">
    <div id="cart" class="btn-group btn-block">
        <!--<span class="cart-icon"></span>-->
        <button type="button" data-toggle="dropdown" data-loading-text="Loading..." class="btn btn-inverse btn-block btn-lg dropdown-toggle">
            <!-- <span class="heading-title">from the Blogs</span>-->
            <span id="cart-total">
                @Model.FavoriteProductsCount
            </span>
            <span id="cart-title">
                your wishlist
                <i class="fa fa-angle-down"></i>
            </span>
        </button>

        <ul class="dropdown-menu pull-right cart-menu">
            <li>
                <p class="text-center">
                    @if (@Model.FavoriteProductsCount > 0)
                    {
                        <a asp-controller="Favorites" asp-action="All">
                            You have @Model.FavoriteProductsCount @(Model.FavoriteProductsCount > 1 ? "items" : "item")
                        </a>
                    }
                    else
                    {
                        <span>
                            Your wishlist is empty!
                        </span>
                    }
                </p>
            </li>
        </ul>
    </div>
</div>
