@model IEnumerable<BulgarianWines.Data.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE html>
<head>
    <meta charset="utf-8" />
</head>

@foreach (var category in Model)
{
    <div>
        @category.Name
    </div>
}
